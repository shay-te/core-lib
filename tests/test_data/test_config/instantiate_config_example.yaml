config:
  _target_: tests.test_instantiate_config.ExampleCoreLib
  conf:
    db:
      log_queries: false
      create_db: true
      session:
        pool_recycle: 3600
        pool_pre_ping: false
      url:
        protocol: sqlite