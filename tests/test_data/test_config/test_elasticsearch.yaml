core_lib:
  elasticsearch:
    _target_: core_lib.connection.elasticsearch_connection_factory.ElasticSearchConnectionFactory
    config:
      url:
        protocol: http
        host: localhost
        port: 9200
