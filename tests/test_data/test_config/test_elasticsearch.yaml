core_lib:
  elasticsearch:
    _target_: core_lib.connection.elasticsearch_connection_registry.ElasticSearchConnectionRegistry
    config:
      url:
        protocol: http
        host: ${oc.env:ELASTIC_HOST}
        port: ${oc.env:ELASTIC_PORT}
        username: ${oc.env:ELASTIC_USER}
        password: ${oc.env:ELASTIC_PASSWORD}
